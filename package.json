{
  "name": "ai-convo-records",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "ensure-conversations": "node scripts/ensure-conversations-file.js",
    "predev": "npm run ensure-conversations",
    "prebuild": "npm run ensure-conversations",
    "dev": "vite",
    "build": "tsc && vite build",
    "import-convos": "node scripts/import-conversations.js",
    "preview": "vite preview",
    "lint": "eslint . --ext ts,tsx --report-unused-disable-directives --max-warnings 0"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.43",
    "@types/react-dom": "^18.2.17",
    "@vitejs/plugin-react": "^4.2.1",
    "typescript": "^5.3.3",
    "vite": "^5.0.8"
  }
}
